package com.marxist.leftwing_community.generator;

import com.baomidou.mybatisplus.generator.AutoGenerator;
import com.baomidou.mybatisplus.generator.config.DataSourceConfig;
import com.baomidou.mybatisplus.generator.config.GlobalConfig;
import com.baomidou.mybatisplus.generator.config.PackageConfig;
import com.baomidou.mybatisplus.generator.config.rules.DateType;

/**
 * MyBatisPlus逆向工程
 * MyBatisPlus代码生成器：3.4.1版本语法
 */
@Deprecated//已过时
public class MyBatisPlusGenerator {
    public static void main(String[] args) {
        /*

        //自动配置类
        AutoGenerator autoGenerator = new AutoGenerator();

        //添加数据源
        DataSourceConfig dataSources = new DataSourceConfig();
        dataSources.setDriverName("com.mysql.cj.jdbc.Driver");
        dataSources.setUrl("jdbc:mysql://localhost:3306/marxism_blog");
        dataSources.setUsername("root");
        dataSources.setPassword("123456");

        //全局配置
        GlobalConfig gc = new GlobalConfig();

        String projectPath = System.getProperty("user.dir");//获取项目路径及代码生成器所在的项目路径（通用写法）
        String outputPath = projectPath + "/src/main/java/";//输出地址
        System.out.println(outputPath);

        gc.setOutputDir(outputPath);//设置输出文件路径
        gc.setAuthor("@MatikaneSpartakusbund");//作者
        gc.setOpen(true);//执行完是否打开输出的目录，默认true
        gc.setFileOverride(true);//覆盖已有的文件，默认false(第一次生成时放开)
        gc.setBaseResultMap(true);
        gc.setBaseColumnList(true);
        gc.setDateType(DateType.ONLY_DATE);//设置日期类型为Date(若不设置时间类型都会变成LocalDateTime部分连接池例如druid是无法识别的)

        //包配置
        PackageConfig pc = new PackageConfig();
        //模块名称
        //pc.setModuleName(scanner("模块名"));
        //设置父包
        pc.setParent("com.marxist.leftwing_community");
        //自定义实体包名(不同的模块自己手动修改)
        pc.setEntity("entity");
        //自定义mapper包名(不同的模块自己手动修改)
        pc.setMapper("dao");
        //自定义mapper.xml包名(不同的模块自己手动修改)
        pc.setXml("dao.mapper");
        //自定义service包名(不同的模块自己手动修改)
        pc.setService("service");
        //自定义serviceImpl包名(不同的模块自己手动修改)
        pc.setServiceImpl("service.impl");
        //自定义controller包名(不同的模块自己手动修改)
        pc.setController("controller");

        //设置全局配置
        autoGenerator.setGlobalConfig(gc);
        //设置数据源配置
        autoGenerator.setDataSource(dataSources);
        //包配置
        autoGenerator.setPackageInfo(pc);

        //执行代码生成器
        autoGenerator.execute();

         */

    }

}
